"""nist.* namespace — NIST ASD energy levels + transitions, CODATA constants,
and the curated Dirac-target precision splittings registry.

Migrated under the umbrella precision_data_mcp per issue
[#99](https://github.com/temoTxt/PyPhysics/issues/99); originally shipped via
[issue #90](https://github.com/temoTxt/PyPhysics/issues/90) as the standalone
``nist_mcp`` server.

The four tools (registered in ``precision_data_mcp.server`` with the ``nist.``
prefix) preserve the original signatures, cache semantics, and return shape
verbatim — the migration is purely about server boundary, not behaviour.
"""
